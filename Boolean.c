#include <stdio.h>
#include <stdbool.h>
int main() {
    // By-default there's no boolean data type in C
    // We use <stdbool.h>
    // true = 1, false = 0
    bool status = true; // 1 0
    printf("%d", status);
    return 0;
}
